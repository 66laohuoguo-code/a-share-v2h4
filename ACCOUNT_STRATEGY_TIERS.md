# 账户资金档与周调仓策略

本文说明周调仓程序按账户实际总资产自动选择的正式策略。总资产等于最新未复权收盘价计算的股票市值加 `positions.csv` 中的可用现金；账户名称不会影响策略选择。

| 当前账户总资产 | 自动配置 | 回测验证资金 | 策略特点 |
|---:|---|---:|---|
| 低于 5 万元 | `v22s_20k_entry_weight_monthly_06_official.json` | 2 万元 | 12 只股票、小账户整手约束、风险叠加、Q90 集合竞价；月度行业信号影响新股票入场与目标权重，但不单独触发旧股票退出。 |
| 5 万元至低于 30 万元 | `v22r3_weekly_100k_official.json` | 10 万元 | 20 只股票、V2.2 R3 时点化价值/质量 Alpha、UNKNOWN 行业 5% 上限、Q97.5 集合竞价。 |
| 30 万元至低于 75 万元 | `v22r3_weekly_560k_official.json` | 56 万元 | 20 只股票、V2.2 R3 时点化价值/质量 Alpha、UNKNOWN 行业 5% 上限、Q97.5 集合竞价。 |
| 75 万元及以上 | `v22r3_weekly_1m_official.json` | 100 万元 | 20 只股票、V2.2 R3 时点化价值/质量 Alpha、UNKNOWN 行业 5% 上限、Q97.5 集合竞价。 |

超过 100 万元的账户沿用 100 万元档，属于容量外推；周报会保留相应警告。需要固定某个策略做研究时，才传入 `-StrategyConfig`。日常实盘辅助流程不要传该参数，避免账户金额变化后仍使用错误档位。

## 2 万元档的行业信号

2 万元策略使用行业 20/60 日相对趋势构造最多 6% 的行业卫星。它只在市场风险预算允许时生效，并在每个自然月的第一个周末决策日冻结行业名单；同月后续周五继续沿用该名单。这样实时周调仓与回测一致，不会因每周临时追逐行业而增加不必要换手。

该策略在 2010-2020 留出期和 2021-2026 近期样本中均优于此前 2 万元正式版，但历史回测不保证未来收益。交易前仍需检查订单、可用现金、停牌、涨跌停、集合竞价价格和券商规则。

## 每周如何使用

1. 更新各账户自己的 `data/input/accounts/<AccountId>/positions.csv`，包括实际持股、可用现金和已成交结果。
2. 将本周行情 Excel 放入 `data/raw/market_data/`。
3. 运行 `run_weekly.ps1`，传入账户 ID、数据库、风险数据库和当前年份。
4. 在生成的 `summary` 中确认 `capital_strategy_tier` 和 `strategy_config`；它们必须与本表对应。
5. 审核 `orders`，以券商真实可成交价格和可用资金为准，程序不会自动下单。

所有账户各自拥有独立的 `account_state.json`。不要把一个账户的状态文件或持仓文件复制给另一个账户。
